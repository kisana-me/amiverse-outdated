<% images.each do |image| %>
  <%= f.check_box selecteds, { multiple: true }, image.aid, nil %>
  <%= f.label "#{selecteds}_#{image.aid}", image.name %>
  <%= image_tag image.image_url, class: 'storage-images-image' %>
<% end %>