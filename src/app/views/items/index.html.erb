<% provide(:layout_type, 'header_text') %>
<% provide(:header_text, 'TL') %>
<% provide(:title, 'TL') %>

<div id="items">
  <% @items.each do |item| %>
    <%= render 'items/item', item: item, reactions: @reactions %>
  <% end %>
</div>
<%= link_to "New item", new_item_path %>